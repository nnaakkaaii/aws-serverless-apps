version: 1
applications:
  - appRoot: apps/todos
    frontend:
      phases:
        build:
          commands:
            - yarn install
            - yarn nx build todos
      artifacts:
        baseDirectory: ../../dist/apps/todos
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
